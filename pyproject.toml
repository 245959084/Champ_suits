[project]
name = "champ-suits"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy>=2.4.6",
    "pandas>=0.3.1",
    "streamlit>=1.58.0",
]

[dependency-groups]
dev = [
    "ruff>=0.15.16",
]

[project.scripts]
champ-suits = "main:main"
